# #492 — Activates the `/__overflow/:component` route shim in
# `src/router.tsx`. Used by the `dashboard-overflow` Playwright job's
# `vite build --mode overflow` step. A normal `vite build` (no mode
# arg) does NOT load this file; the production bundle ships zero bytes
# of overflow-shim code.
VITE_OVERFLOW=1
