{
  "compilerOptions": {
    "target": "ES2020",
    "module": "commonjs",
    "outDir": "../dist/scripts",
    "rootDir": ".",
    "esModuleInterop": true,
    "resolveJsonModule": false,
    "strict": false,
    "noImplicitAny": false
  },
  "include": ["*.ts"],
  "comment": "enumerate-tool-names.ts imports ../src (for the surface-drift gate) and is run via `npx tsx`, never compiled to dist/scripts — excluded so its src import doesn't drag all of src under this rootDir.",
  "exclude": ["enumerate-tool-names.ts"]
}
